[project]
name = "tunnel"
version = "0.1.0"
description = "Self-hosted caBLE tunnel relay (cable/new + cable/connect)"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "websockets>=16.0",
]
